info:
  name: DeletarLivro
  type: http

http:
  method: DELETE
  url: http://localhost:3000/livros/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
